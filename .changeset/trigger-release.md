---
---

Add version-packages script and update release workflow to use changesets for scaffolder releases. The scaffolder is now versioned alongside template changes — bump its version to publish. No runtime changes to scaffolded libraries.