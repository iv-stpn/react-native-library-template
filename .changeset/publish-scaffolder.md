---
---

Release scaffolder version 0.1.5 with changeset workflow integration. The scaffolder is now versioned and published automatically when changesets are consumed. No runtime changes to scaffolded libraries.