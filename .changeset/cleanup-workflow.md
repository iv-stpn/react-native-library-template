---
---

Remove release-scaffolder.yml workflow and simplify release logic: scaffolder is now published directly when its version changes. Clean up irrelevant template release code; scaffolded projects get changeset workflow. No runtime changes to scaffolded libraries.